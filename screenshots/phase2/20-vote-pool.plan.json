{
  "out": "screenshots/phase2/20-vote-pool",
  "shots": [
    {
      "name": "01-your-votes-pool",
      "steps": [
        {
          "go": "/reshoot?to=/dashboard"
        },
        {
          "eval": "localStorage.setItem('asc.theme','dark'); document.documentElement.removeAttribute('data-theme')"
        },
        {
          "wait": 1500
        },
        {
          "eval": "(() => { const h = [...document.querySelectorAll('h2')].find(x => /Your votes/.test(x.textContent)); h.scrollIntoView({ block: 'start', behavior: 'instant' }); })()"
        },
        {
          "wait": 500
        },
        {
          "eval": "(() => { const t = [...document.querySelectorAll('h2')].find(x => /Your votes/.test(x.textContent)).closest('section').innerText; if (!/you have voted/i.test(t)) throw new Error('no pool header'); if (!t.includes('100,000')) throw new Error('total is not 100,000: ' + t.slice(0, 120)); if (!/% of your votes/.test(t)) throw new Error('no shares'); })()"
        }
      ]
    },
    {
      "name": "02-raised-one-vote",
      "steps": [
        {
          "go": "/reshoot?to=/dashboard"
        },
        {
          "eval": "localStorage.setItem('asc.theme','dark'); document.documentElement.removeAttribute('data-theme')"
        },
        {
          "wait": 1500
        },
        {
          "eval": "(() => { const h = [...document.querySelectorAll('h2')].find(x => /Your votes/.test(x.textContent)); h.scrollIntoView({ block: 'start', behavior: 'instant' }); })()"
        },
        {
          "wait": 400
        },
        {
          "eval": "(() => { const b = [...document.querySelectorAll('h2')].find(x => /Your votes/.test(x.textContent)).closest('section').querySelector('button[aria-label^=\"Raise your vote for Orrery\"]'); if (!b) throw new Error('no plus on Orrery'); b.click(); b.click(); })()"
        },
        {
          "wait": 1800
        },
        {
          "eval": "(() => { const t = [...document.querySelectorAll('h2')].find(x => /Your votes/.test(x.textContent)).closest('section').innerText; if (!t.includes('115,000')) throw new Error('total did not move to 115,000: ' + t.slice(0, 160)); if (!/saved/i.test(t)) throw new Error('no Saved'); if ([...document.querySelectorAll('h2')].find(x => /Your votes/.test(x.textContent)).closest('section').querySelector('button[aria-label^=\"Raise your vote for Aurelia\"]')) throw new Error('a settled vote still has levers'); })()"
        }
      ]
    },
    {
      "name": "03-vote-persisted",
      "capture": false,
      "steps": [
        {
          "go": "/reshoot?to=/dashboard"
        },
        {
          "eval": "localStorage.setItem('asc.theme','dark'); document.documentElement.removeAttribute('data-theme')"
        },
        {
          "wait": 1400
        },
        {
          "eval": "(() => { const h = [...document.querySelectorAll('h2')].find(x => /Your votes/.test(x.textContent)); h.scrollIntoView({ block: 'start', behavior: 'instant' }); })()"
        },
        {
          "eval": "(() => { const b = [...document.querySelectorAll('h2')].find(x => /Your votes/.test(x.textContent)).closest('section').querySelector('button[aria-label^=\"Lower your vote for Orrery\"]'); b.click(); })()"
        },
        {
          "wait": 1600
        },
        {
          "eval": "fetch('/api/radar').then(r => r.json()).then(j => { const list = Array.isArray(j) ? j : (j.data ?? j.items ?? []); const o = list.find(c => c.slug === 'orrery'); if (!o || o.yourAmount !== 20000) throw new Error('server has ' + (o && o.yourAmount)); })"
        }
      ]
    },
    {
      "name": "04-your-votes-daylight",
      "steps": [
        {
          "go": "/reshoot?to=/dashboard"
        },
        {
          "eval": "localStorage.setItem('asc.theme','light'); document.documentElement.setAttribute('data-theme','light')"
        },
        {
          "wait": 1500
        },
        {
          "eval": "(() => { const h = [...document.querySelectorAll('h2')].find(x => /Your votes/.test(x.textContent)); h.scrollIntoView({ block: 'start', behavior: 'instant' }); })()"
        },
        {
          "wait": 500
        }
      ]
    }
  ]
}