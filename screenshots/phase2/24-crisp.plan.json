{
  "out": "screenshots/phase2/24-crisp",
  "sizes": [
    [
      1600,
      1000
    ]
  ],
  "shots": [
    {
      "name": "dashboard-ember",
      "steps": [
        {
          "go": "/reshoot?to=/dashboard"
        },
        {
          "eval": "localStorage.setItem('asc.theme','dark'); document.documentElement.removeAttribute('data-theme')"
        },
        {
          "wait": 1800
        }
      ]
    },
    {
      "name": "deal-ember",
      "steps": [
        {
          "go": "/reshoot?to=/deals/calder"
        },
        {
          "eval": "localStorage.setItem('asc.theme','dark'); document.documentElement.removeAttribute('data-theme')"
        },
        {
          "wait": 1800
        },
        {
          "eval": "(() => { const h = [...document.querySelectorAll('h2, h3')].find(x => /what it costs/i.test(x.textContent)); if (h) h.scrollIntoView({ block: 'start', behavior: 'instant' }); })()"
        },
        {
          "wait": 900
        },
        {
          "eval": "(() => { const vh = innerHeight; const arrived = [...document.querySelectorAll('section')].filter(s => { const r = s.getBoundingClientRect(); return r.height > 0 && r.top < vh - 340; }); if (arrived.length === 0) throw new Error('no section has arrived to check'); const bad = arrived.filter(s => { const t = getComputedStyle(s).transform; return t && t !== 'none'; }); if (bad.length > 0) throw new Error(bad.length + ' of ' + arrived.length + ' arrived sections still carry a transform'); })()"
        }
      ]
    },
    {
      "name": "dashboard-ice",
      "steps": [
        {
          "go": "/reshoot?to=/dashboard"
        },
        {
          "eval": "localStorage.setItem('asc.theme','ice'); document.documentElement.setAttribute('data-theme','ice')"
        },
        {
          "wait": 1800
        }
      ]
    },
    {
      "name": "deal-ice",
      "steps": [
        {
          "go": "/reshoot?to=/deals/calder"
        },
        {
          "eval": "localStorage.setItem('asc.theme','ice'); document.documentElement.setAttribute('data-theme','ice')"
        },
        {
          "wait": 1800
        },
        {
          "eval": "(() => { const h = [...document.querySelectorAll('h2, h3')].find(x => /what it costs/i.test(x.textContent)); if (h) h.scrollIntoView({ block: 'start', behavior: 'instant' }); })()"
        },
        {
          "wait": 900
        },
        {
          "eval": "(() => { const vh = innerHeight; const arrived = [...document.querySelectorAll('section')].filter(s => { const r = s.getBoundingClientRect(); return r.height > 0 && r.top < vh - 340; }); if (arrived.length === 0) throw new Error('no section has arrived to check'); const bad = arrived.filter(s => { const t = getComputedStyle(s).transform; return t && t !== 'none'; }); if (bad.length > 0) throw new Error(bad.length + ' of ' + arrived.length + ' arrived sections still carry a transform'); })()"
        }
      ]
    },
    {
      "name": "dashboard-daylight",
      "steps": [
        {
          "go": "/reshoot?to=/dashboard"
        },
        {
          "eval": "localStorage.setItem('asc.theme','light'); document.documentElement.setAttribute('data-theme','light')"
        },
        {
          "wait": 1800
        }
      ]
    },
    {
      "name": "deal-daylight",
      "steps": [
        {
          "go": "/reshoot?to=/deals/calder"
        },
        {
          "eval": "localStorage.setItem('asc.theme','light'); document.documentElement.setAttribute('data-theme','light')"
        },
        {
          "wait": 1800
        },
        {
          "eval": "(() => { const h = [...document.querySelectorAll('h2, h3')].find(x => /what it costs/i.test(x.textContent)); if (h) h.scrollIntoView({ block: 'start', behavior: 'instant' }); })()"
        },
        {
          "wait": 900
        },
        {
          "eval": "(() => { const vh = innerHeight; const arrived = [...document.querySelectorAll('section')].filter(s => { const r = s.getBoundingClientRect(); return r.height > 0 && r.top < vh - 340; }); if (arrived.length === 0) throw new Error('no section has arrived to check'); const bad = arrived.filter(s => { const t = getComputedStyle(s).transform; return t && t !== 'none'; }); if (bad.length > 0) throw new Error(bad.length + ' of ' + arrived.length + ' arrived sections still carry a transform'); })()"
        }
      ]
    },
    {
      "name": "marketplace-ember",
      "steps": [
        {
          "go": "/reshoot?to=/marketplace"
        },
        {
          "eval": "localStorage.setItem('asc.theme','dark'); document.documentElement.removeAttribute('data-theme')"
        },
        {
          "wait": 1800
        },
        {
          "scroll": "#open-now"
        },
        {
          "wait": 500
        }
      ]
    }
  ]
}