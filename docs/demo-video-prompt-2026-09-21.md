# Prompt for the product demo video chat

Paste everything below the line into the Claude chat that owns the demo brief, narration and scenes. Attach `docs/demo-brief-update-2026-09-21.md` and the screenshots named in step 1.

---

You wrote the brief, narration and scene list for the AltSpot product demo video against the platform as it stood around September 14 to 16. The platform has changed a great deal since then: about 120 commits, including several compliance corrections, a rebuilt design system, and new features. I need you to rebuild the brief, the narration and the scene list so they match the platform as it is today, September 21.

**Your sources, in order of authority**

1. The attached screenshots. They are the platform as it is right now, taken from the build we will record from, straight after a data reset, so every screen shows the exact demo account the film will use. There are three folders, one per theme (`ember`, `ice`, `daylight`), with identical file names in each; `NAME-1600x1000.png` is the first screen on arrival and `NAME-full.png` is the whole page top to bottom. **Work from the `ember` folder.** Ember is the default theme and the one we record in. Open the other two only for the short theme-switch beat.
2. The attached document `demo-brief-update-2026-09-21.md`. Section 1 is a list of seventeen things the old brief now gets wrong, several of them legal. Fix every one of them first. Section 2 is what the film may and may not show or say. Section 3 is the demo account: the cast, the numbers and the dates on screen. Section 4 is every new feature with why it matters and a narration angle. Sections 6 and 7 are a suggested scene spine with click paths and the recording checklist. Section 8 lists what is still pending counsel and must stay cuttable.
3. Your existing brief, for whatever still holds. Where it disagrees with the two sources above, the sources win.

**What I want back, in this order**

1. **A change log against your old brief**: every scene, line or claim you are removing or changing, and why. Be specific, because a few of these are compliance points and I need to be able to check them.
2. **The rebuilt brief**: positioning, audience, the promise of the film, the rails.
3. **The scene list**, numbered, each with: the exact click path (use the paths in the brief update), the screenshot file it corresponds to, what the viewer sees, the narration for that scene, and its approximate duration. Target the same total length as before unless you think the new material justifies more, in which case say so.
4. **The full narration**, clean, as one read.
5. **A list of shots that need the presenter to do something live** (press a button, drag a scale, hover a bar) that a still cannot show, so I know what to rehearse.

**Rules for anything you write**

- No em dashes anywhere. Use commas, periods or colons.
- Short sentences. Specific about process and structure. Never about returns, never a projection, never a probability.
- Say **escrow**, never "funded", for a member's own money. Say a vote **reserves nothing and moves no money**. Never call the votes a fund, a balance or an allocation.
- Say **signed** when documents are signed, **in escrow** when money has moved, **invested** only once a deal has closed. The platform now makes this distinction everywhere and the narration must match it.
- The guide is called **Spot**. It explains and never advises.
- Never a fee expressed as a percentage of capital raised. The fee is a flat $10,000 formation and administration fee per SPV, shared pro rata, plus 1% a year of committed capital, five years funded once at closing. Never a carry number.
- No real company names. Every company on the platform is invented; use the names in the screenshots.
- No calendar dates in narration. Dates on screen are relative to recording day. Say "three days left", not a date.
- The illustrative scenarios scene and the disclosures page must each be a single scene that can be cut without breaking the flow, because both are pending counsel.
- Do not script a partner console, a referral reward, "verified" accreditation, a waiting period, a liquidity window or a trading screen. None of those exist.
- Never name a real vendor, bank or custodian. The platform now says "an escrow account in the SPV's own name at a U.S. bank" and "a secure bank link".
- Portfolio's "invested" is $143,000 of positions in closed deals. The $25,000 in escrow is a separate line and must never be added into it in narration.
- The Read step: frame the right-hand confirmation panel. The document body still carries the specimen's economics under a specimen notice.

**Things to look at closely in the screenshots, because they are new since your brief**

- `10-dashboard`: Needs you at the top, the vote pool ("You have voted $75,000") with Adjust all, and the Watchlist showing real stages ("Send by", "In escrow").
- `11-marketplace-shelf`: every company has its own logo and a stage pill with a five-bar maturity meter; the Aurelia card says "In escrow", with a band "$25,000 in escrow, until the deal closes". It says "You invested" only once a deal has closed.
- `12-marketplace-radar` and `47`, `48`: the Radar sort (Featured, Most voted, Rising, New), Details, the vote scale, Remove vote.
- `13-deal-calder` and `55`: the funding header and "What it costs" as four cards. `54` is the scenarios scene (cuttable). `14-deal-aurelia`: what a deal looks like when you are already in it.
- `16` to `18`: checkout. `18` is "Your allocation is reserved" with the ten-day clock (You signed, Send by, 3 of 10 days).
- `19-portfolio` and `53`: the plain sentence at the top, the capital account, the new per-position chart. `20-portfolio-statement`: the one-page statement to save as a PDF.
- `22-terminal`: For you with reasons, the live bar, Media and content.
- `40` to `43`: the bell panel, the ten-day clock in Steps, Adjust all, and a vote row opening the company overview.
- `50` and `51`: Spot answering a fee question with a picture, and Spot declining "how much should I invest".
- `29-settings`: Appearance (three themes, Ember default) and the invite card "Bring someone into the room", which promises nothing of value for an introduction. That wording is deliberate and legal; keep it.
- `30-disclosures` and the footer on every page: the trust layer. Show briefly or not at all.

Start with the change log. If anything in the screenshots contradicts the brief update document, tell me rather than guessing.
